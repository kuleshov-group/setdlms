_target_: src.backbone.dit_legacy.DITLegacy
length: ${model.config.length}
causal_attention: false
adaln: false
hidden_size: -1
cond_dim: 128
n_heads: -1
num_layers: -1
dropout: 0.1
tie_word_embeddings: true
attn_backend: sdpa
vocab_size: -1
block_size: ${block_size}
pretrained_model_name_or_path: ${pretrained_model_name_or_path}
