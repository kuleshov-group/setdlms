defaults:
  - mdlm@_here_
  - _self_

_target_: src.denoiser.BD3LM
config:
  _target_: src.denoiser.BD3LMConfig
  block_size: 4
  attn_backend: "sdpa" # "flex_attention"
  backbone_is_decoder_only: false
