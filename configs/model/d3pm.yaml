defaults:
  - denoiser@_here_
  - _self_

_target_: src.denoiser.D3PM
config:
  _target_: src.denoiser.D3PMConfig
  T: 1000  # T > 0 indicates finite-time vs. continuous (e.g., MDLM, UDLM), i.e., T = 0.
  diffusion_type: ???  # e.g., "absorbing" (MDLM), "uniform" (UDLM)
